import { Location } from '../../types/data';

type MapProps = {
  points: Location[];
}

function Map({points}: MapProps):JSX.Element {
  return <section className="cities__map map">djdjd</section>;
}

export default Map;
